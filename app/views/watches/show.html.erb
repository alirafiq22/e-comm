<p id="notice"><%= notice %></p>

<table>
	<tr>
		<th>Company</th>
		<th>Model</th>
		<th>Quantity</th>
		<th>Owner</th>
	</tr>
	<tr>
		<td><%= @watch.company %></td>
		<td><%= @watch.mno %></td>
		<td><%= @watch.qty %></td>
		<td><%= @watch.owner %></td>
	</tr>
</table>
<% if seller_signed_in? && current_seller.id == @watch.owner %>
	<%= link_to 'Edit', edit_watch_path(@watch) %> |
<% end %>
<%= link_to 'Back', watches_path %>
